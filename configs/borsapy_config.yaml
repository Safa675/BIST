borsapy:
  use_mcp_fallback: true
  mcp_endpoint: "https://borsamcp.fastmcp.app/mcp"
  circuit_breaker:
    failure_threshold: 3
    recovery_timeout: 30
  retry_policy:
    max_retries: 3
    base_delay: 1
    max_delay: 10
    backoff_factor: 2
    jitter: true
  timeout: 15.0
